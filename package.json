{
  "name": "proyecto-json-server",
  "version": "1.0.0",
  "description": "Armaremos un pequeño servidor de cursos",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "Omar Reyes",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
